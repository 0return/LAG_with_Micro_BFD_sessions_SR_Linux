set / interface lag 10
set / interface lag 10 admin-state enable
set / interface lag 10 subinterface 0
set / interface lag 10 subinterface 0 ipv4
set / interface lag 10 subinterface 0 ipv4 admin-state enable
set / interface lag 10 subinterface 0 ipv4 address 10.10.10.2/30
set / interface lag 10 lag


set / interface ethernet-1/1
set / interface ethernet-1/1 admin-estate enable
set / interface ethernet-1/1 ethernet
set / interface ethernet-1/1 ethernet aggregate-id lag10

set / interface ethernet-1/2
set / interface ethernet-1/2 admin-estate enable
set / interface ethernet-1/2 ethernet
set / interface ethernet-1/2 ethernet aggregate-id lag10

set / interface ethernet-1/3
set / interface ethernet-1/3 admin-estate enable
set / interface ethernet-1/3 ethernet
set / interface ethernet-1/3 ethernet aggregate-id lag10

set / interface ethernet-1/4
set / interface ethernet-1/4 admin-estate enable
set / interface ethernet-1/4 ethernet
set / interface ethernet-1/4 ethernet aggregate-id lag10

set / bfd
set / bfd admin-estate enable
set / bfd micro-bfd-sessions
set / bfd micro-bfd-sessions lag-interface lag10
set / bfd micro-bfd-sessions lag-interface lag10 local-address 10.1.1.2
set / bfd micro-bfd-sessions lag-interface lag10 remote-address 10.1.1.1
set / bfd micro-bfd-sessions lag-interface lag10 desired-minimum-transmit-interval 10000
set / bfd micro-bfd-sessions lag-interface lag10 required-minimum-receive 100000

set / network-instance default
set / network-instance default admin-state enable
set / network-instance default type default
set / network-instance default lag10.0
